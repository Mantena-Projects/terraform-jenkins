---
- name: Install HashiCorp repository and Terraform
  hosts: ec2_instances
  become: true
  tasks:
    - name: Add HashiCorp GPG key
      apt_key:
        url: https://apt.releases.hashicorp.com/gpg
        state: present

    - name: Add HashiCorp repository
      apt_repository:
        repo: "sudo deb [arch=amd64] https://apt.releases.hashicorp.com focal main"
        state: present

    - name: Update package list
      apt:
        update_cache: yes

    - name: Install Terraform
      apt:
        name: terraform
        state: present
